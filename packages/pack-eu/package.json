{
  "name": "@clipcloak/pack-eu",
  "version": "0.2.0",
  "description": "European PII detectors (IBAN, VAT) for ClipCloak",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "test": "vitest run"
  },
  "dependencies": {
    "@clipcloak/core": "workspace:*"
  }
}
