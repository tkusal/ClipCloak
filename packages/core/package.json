{
  "name": "@clipcloak/core",
  "version": "0.2.0",
  "description": "Core detection engine for ClipCloak",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "test": "vitest run"
  }
}
